React has SPA behaviour.
SPA - Single Page Application

 - means application will be loaded once , after that even if we change the route it won't trigger reload .

Routing in React
- npm i react-router-dom
- wrap whole <App> inside <BrowseRouter>
means - <BrowserRouter> <App> </BrowserRouter>
 Routes - Route

